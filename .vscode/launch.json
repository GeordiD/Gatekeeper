{
    "configurations": [
        {
            "name": "Docker .NET Core Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "netCore": {
                "appProject": "${workspaceFolder}/ThetaGangTracker/ThetaGangTracker.csproj"
            },
            "launchBrowser": {
                "enabled": false
            }
        }
    ]
}
